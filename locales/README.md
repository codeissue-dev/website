# Locale resources

`locales/en/common.json` and `locales/ru/common.json` are the only runtime translation sources.

The historical files under `dictionaries/` and `app/i18n/locales/` are retained only because package snapshots preserve input paths. They contain deprecation markers and are not imported by the application.
